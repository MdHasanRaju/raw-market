import React from 'react';
import LoginAuth from '../../components/authentication/Login';
const Login = () => {
    return (
        <div>
            <LoginAuth></LoginAuth>
        </div>
    );
};

export default Login;